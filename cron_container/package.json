{
  "name": "cron_container",
  "version": "1.0.1",
  "description": "npm dependencies for the file-deleter script to run on cron schedule/",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1",
    "mongoose": "^6.4.7",
    "pg": "^8.7.3"
  }
}
